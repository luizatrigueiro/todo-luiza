<h1><%= @list.title %></h1>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Go back', lists_path %>

<h2>Tasks</h2>

<%= link_to 'Add Task', new_list_task_path(@list), class: "btn btn-primary" %>

<ul>
  <% @list.tasks.each do |task| %>
    <li>
      <%= task.description %>
      <% if task.done %>
        ✅
      <% end %>
      |
      <%= link_to 'Edit', edit_list_task_path(@list, task) %>
      |
      <%= link_to 'Delete', list_task_path(@list, task), method: :delete, data: { confirm: 'Are you sure?' } %>
    </li>
  <% end %>
</ul>
